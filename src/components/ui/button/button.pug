mixin Button (props)
    -
        const { label, JClass, url, type } = props || {}
    if url 
        if type
            a.link__emit(href=url)=label
        else 
            a(href=url)=label
    else
        button.btn(class=JClass)=label